// Re-export for convenience
// Use these functions instead of direct imports
export { createClient as createBrowserClient } from './supabase/client'
export { createClient as createServerClient } from './supabase/server'
